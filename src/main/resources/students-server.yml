# Spring properties
spring:
  application:
     name: students-service
  freemarker:
    enabled: false
  thymeleaf:
    cache: false
    prefix: classpath:/students-server/templates/

error.path: /error

server.port: 4444

eureka:
  client:
    serviceUrl:
      defaultZone: http://${registration.server.hostname}:1111/eureka/
  instance:
    leaseRenewalIntervalInSeconds: 10
management.endpoints.web.exposure.include: '*'

