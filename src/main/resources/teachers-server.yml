# Spring properties
spring:
  application:
     name: teachers-service
  freemarker:
    enabled: false
  thymeleaf:
    cache: false
    prefix: classpath:/teachers-server/templates/

error.path: /error

server.port: 3333

eureka:
  client:
    serviceUrl:
      defaultZone: http://${registration.server.hostname}:1111/eureka/
  instance:
    leaseRenewalIntervalInSeconds: 10
management.endpoints.web.exposure.include: '*'

